# Private subscription sources not tracked in git.
# Copy this to config/private.yaml and fill in actual URLs.
#
# The file is gitignored — only config/private-example.yaml is tracked.

subscriptions:
  sources:
    # - name: my-private-source
    #   url: https://example.com/private/sub
