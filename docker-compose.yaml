services:
  sub-preprocessor:
    build:
      context: .
      dockerfile: Dockerfile
    image: sub-preprocessor:latest
    container_name: sub-preprocessor
    ports:
      - "7008:8080"
    volumes:
      - ./config:/config:ro
    restart: always

  gemini-block-checker:
    image: alpine:3.21
    container_name: gemini-block-checker
    network_mode: host
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        apk add --no-cache curl bash bind-tools
        wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
        chmod +x /usr/local/bin/yq
        export GEMINI_API_KEY="$(sed -n 's/^LITELLM_GOOGLE_API_KEY=//p' /run/agenix/litellm-env)"
        exec /scripts/asn-block-check.sh
    volumes:
      - ./config:/config:rw
      - ./scripts:/scripts:ro
      # agenix-decrypted secret; key read from it at container start (survives reboot/restart)
      - /run/agenix/litellm-env:/run/agenix/litellm-env:ro
    environment:
      - INTERVAL=300
      - CONFIG=/config/config.yaml
    restart: unless-stopped

  tg-sub-crawler:
    image: alpine:3.21
    container_name: tg-sub-crawler
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        apk add --no-cache bash curl grep sed coreutils
        wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
        chmod +x /usr/local/bin/yq
        exec bash /scripts/tg-sub-crawler.sh
    volumes:
      - ./config:/config:rw
      - ./scripts:/scripts:ro
    environment:
      - CHANNEL=o00000000i
      - PRIVATE=/config/private.yaml
      - INTERVAL=1800
      - PAGES=6
      - PRUNE=1
    restart: unless-stopped
